<?php
/**
 * @file
 * Code for the Race feature.
 */

include_once 'race.features.inc';

/**
 * Implements hook_node_info().
 */
function race_node_info() {
  $items = array(
    'race' => array(
      'name' => t('Race'),
      'base' => 'race',
      'description' => '',
      'has_title' => '1',
      'title_label' => t('Title'),
      'help' => '',
    ),
  );
  return $items;
}

/**
 * Implementation of hook_form
 * Default base is 'node_content'
 * @param type $node
 * @param type $form_state
 */
function race_form($node, $form_state) {
  //Default base is 'node_content'
  //Title field is not attached otherwise
  $form = node_content_form($node, $form_state);
  return $form;
}